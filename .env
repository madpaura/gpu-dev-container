# =============================================================================
# gpu-dev-container — single source of truth for all environment variables
# Used by: docker-compose-admin.yml, docker-compose-agent.yml, and local dev
# =============================================================================

# -----------------------------------------------------------------------------
# Infrastructure ports
# -----------------------------------------------------------------------------
BACKEND_PORT=8500
FRONTEND_PORT=9000
AGENT_PORT=8510

# -----------------------------------------------------------------------------
# PostgreSQL (admin stack)
# -----------------------------------------------------------------------------
POSTGRES_DB=gpu_dash
POSTGRES_USER=gpu_dash
POSTGRES_PASSWORD=changeme
PG_DATA_VOLUME=./volumes/postgres

# -----------------------------------------------------------------------------
# Admin bootstrap credentials
# -----------------------------------------------------------------------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
ADMIN_EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# Host paths (mounted into containers)
# -----------------------------------------------------------------------------
HOST_HOME=/home/vishwa
HOST_OPT=/opt
WORKDIR_TEMPLATE=/home/vishwa/user-repo/template/
WORKDIR_DEPLOY=/home/vishwa/user-repo/vms/

# -----------------------------------------------------------------------------
# Management server (backend)
# Set to the IP clients use to reach this machine — used in service URL links
# -----------------------------------------------------------------------------
MGMT_SERVER_IP=192.168.68.109

# -----------------------------------------------------------------------------
# Nginx config (must be writable by the backend container at runtime)
# -----------------------------------------------------------------------------
NGINX_CONFIG_FILE=/home/vishwa/llm/gpu-dev-container/backend/nginx/sites-available/dev-services

# -----------------------------------------------------------------------------
# User container image
# -----------------------------------------------------------------------------
DOCKER_IMAGE=gpu-dev-env-test
DOCKER_TAG=latest
DOCKER_HOSTNAME=cxl-dev
DOCKER_CPU=4
DOCKER_CPU_PERCENT=100
DOCKER_MEM_LMT=4g
DOCKER_MEM_SWAP=5g

# -----------------------------------------------------------------------------
# Management server agent settings
# -----------------------------------------------------------------------------
MGMT_SERVER_PORT=8500
AGENT_IP=
AGENTS_LIST=

# -----------------------------------------------------------------------------
# Services inside user containers
# -----------------------------------------------------------------------------
CODE_PORT=8080
CODE_DEFAULT_WORKSPACE=/workspace
CODE_CONFIG_MOUNT=/config
JUPYTER_PORT=8888
SUDO_PASSWORD=abc
GUEST_OS_SSH_PORT=2222
GUEST_OS_SPICE_PORT=3007
GUEST_OS_LIST=

# -----------------------------------------------------------------------------
# Volume mount paths inside user containers
# -----------------------------------------------------------------------------
QVP_BINARY_MOUNT=/opt/qvp
QVP_TAG=0.1
GUEST_OS_MOUNT=/opt/os/guestos
TOOLS_MOUNT=/opt/tools
WORKSPACE_MOUNT=/home/developer/workspace

# -----------------------------------------------------------------------------
# OpenCXL ports (switch, fabric manager, UI)
# -----------------------------------------------------------------------------
OPENCXL_SWITCH_PORT=8000
OPENCXL_FM_PORT=8100
OPENCXL_FM_UI_PORT=3000
